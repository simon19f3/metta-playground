{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && . \"$HOME/.cargo/env\" && rustup target add wasm32-unknown-unknown && cargo install wasm-pack --locked && wasm-pack build --target web && npm run build",
  "outputDirectory": "dist"
}